{
  "$schema": "http://json-schema.org/schema",
  "id": "NgxsSchematics",
  "title": "Generate Boilerplate for NGXS",
  "type": "object",
  "description": "Container-Pattern Facade and Presenter NGXS Schematics",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the components to be created",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of the components?"
    }
  },
  "required": ["name"]
}
